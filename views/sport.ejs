<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        <title><%= sport.title %></title>
        <style>
            body {
                margin: 0;
                display: flex;
                justify-content: center;
                align-items: flex-start;
                height: 100vh;
                background-color: #f7fafc;
                font-family: "Helvetica Neue", sans-serif;
            }
            .session {
                border: 2px solid #e2e8f0;
                border-radius: 0.5rem;
                padding: 1rem;
                display: flex;
                flex-direction: row;
                gap: 10px;
                align-items: center;
                font-size: 1.2rem;
            }
            .sessions {
                display: flex;
                flex-direction: column;
                gap: 1.5rem;
                align-items: center;
            }
            .heading {
                text-align: center;
                margin-bottom: 2rem;
            }
            .links {
                display: flex;
                justify-content: center;
                gap: 2rem;
                margin-top: 2rem;
            }
            .btn {
                padding: 0.5rem 1.5rem;
                border: none;
                border-radius: 0.5rem;
                background-color: #edf2f7;
                color: #4a5568;
                font-weight: bold;
                text-decoration: none;
                transition: all 0.2s ease-in-out;
            }
            .btn:hover {
                background-color: #e2e8f0;
                cursor: pointer;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1 class="heading"><%= sport.title %></h1>
            <div class="sessions">
                <% if (sessionItem.length)  { %>
                    <% for( let i = 0; i < sessionItem.length; i++ ) { %>
                        <div class="session">
                            <div>
                                <h3>Venue:</h3>
                                <p><%= sessionItem[i].venue %></p>
                            </div>
                            <div>
                                <h3>Date:</h3>
                                <p><%= sessionItem[i].dueDate %></p>
                            </div>
                            <div>
                                <h3>Number of Players:</h3>
                                <p><%= sessionItem[i].num_players %></p>
                            </div>
                        </div>
                    <% } %>
                <% } else { %>
                    <p>No sessions available. Create Sessions and come back to this page.</p>
                <% } %>
            </div>
            <div class="links">
                <a href="/sport/<%= sport.id %>/createSession/" class="btn">Create New Session</a>
                <a href="#" class="btn">View Previous Sessions</a>
            </div>
        </div>
    </body>
</html>
